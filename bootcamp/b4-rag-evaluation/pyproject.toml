[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"


[project]
name = "investment-rag-evaluation"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "anthropic",
    "pydantic",
    "python-dotenv"
]


[tool.setuptools]
packages = ["rag_evaluation"]